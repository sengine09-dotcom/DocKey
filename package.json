{
  "name": "doc-key",
  "version": "1.0.0",
  "description": "Doc Key - Document Management System",
  "private": true,
  "scripts": {
    "install:all": "npm install && npm --prefix backend install && npm --prefix frontend install",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev": "echo 'Run in separate terminals:' && echo 'Terminal 1: npm run dev:backend' && echo 'Terminal 2: npm run dev:frontend'",
    "build:backend": "npm --prefix backend run build",
    "build:frontend": "npm --prefix frontend run build",
    "build": "npm run build:backend && npm run build:frontend",
    "backend:start": "node backend/dist/index.js",
    "frontend:start": "npm --prefix frontend run preview"
  },
  "workspaces": [
    "backend",
    "frontend"
  ]
}
